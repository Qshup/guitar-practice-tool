{
  "name": "guitar-practice-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Guitar scale/chord/riff practice tool",
  "scripts": {
    "start": "live-server --port=8080 --no-css-inject --ignore=node_modules,.git --open=index.html",
    "serve": "live-server --port=8080 --no-css-inject --ignore=node_modules,.git --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "pitchy": "^4.1.0",
    "tone": "^15.1.22"
  }
}
